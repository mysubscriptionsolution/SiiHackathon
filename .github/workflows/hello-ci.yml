name: Hello from CI

on:
  push:
    branches:
      - feature/github-actions
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Say Hello
        run: echo "Hello from CI!"
